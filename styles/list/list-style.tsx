import { styled } from '@/styles/stiches.config';

export const Wrapper = styled('div', {
    backgroundImage: 'var(--bg-menu-image)',
    backgroundRepeat: 'no-repeat',
    backgroundSize: 'contain'
});